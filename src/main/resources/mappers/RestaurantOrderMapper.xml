<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jack.backend.dao.RestaurantOrderMapper">

    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        insert into restaurant_order
        (date,
         restaurant_id,
         total_price,
         type,
         completed)
        values (#{date},
                #{restaurant.id},
                #{totalPrice},
                #{type},
                #{completed})
    </insert>

    <resultMap id="RestaurantOrderMapper" type="com.jack.backend.models.RestaurantOrder">
        <id column="id" property="id"/>
        <result column="date" property="date"/>
        <result column="total_price" property="totalPrice"/>
        <result column="type" property="type"/>
        <result column="completed" property="completed"/>
        <association property="restaurant" columnPrefix="restaurant_"
                     javaType="com.jack.backend.models.Restaurant">
            <id column="id" property="id"/>
            <result column="username" property="username"/>
            <result column="password" property="password"/>
            <result column="restaurant_name" property="restaurantName"/>
            <result column="phone_number" property="phoneNumber"/>
            <result column="email" property="email"/>
            <result column="address" property="address"/>
            <result column="description" property="description"/>
            <result column="business_hours" property="businessHours"/>
        </association>
        <collection property="products"
                    select="com.jack.backend.dao.ProductMapper.queryByRestaurantOrder"
                    column="id"
                    ofType="com.jack.backend.models.Product"/>
    </resultMap>

    <select id="queryOrdersByRestaurantId" resultMap="RestaurantOrderMapper">
        select od.id,
               od.date,
               od.total_price,
               od.type,
               od.completed,
               restaurant.id              as restaurant_id,
               restaurant.username        as restaurant_username,
               restaurant.password        as restaurant_password,
               restaurant.restaurant_name as restaurant_restaurant_name,
               restaurant.phone_number    as restaurant_phone_number,
               restaurant.email           as restaurant_email,
               restaurant.address         as restaurant_address,
               restaurant.description     as restaurant_description,
               restaurant.business_hours  as restaurant_business_hours
        from restaurant_order od
                 join restaurants restaurant on restaurant.id = od.restaurant_id
        where od.restaurant_id = #{id}
    </select>

    <select id="query" resultMap="RestaurantOrderMapper">
        select od.id,
               od.date,
               od.total_price,
               od.type,
               od.completed,
               restaurant.id              as restaurant_id,
               restaurant.username        as restaurant_username,
               restaurant.password        as restaurant_password,
               restaurant.restaurant_name as restaurant_restaurant_name,
               restaurant.phone_number    as restaurant_phone_number,
               restaurant.email           as restaurant_email,
               restaurant.address         as restaurant_address,
               restaurant.description     as restaurant_description,
               restaurant.business_hours  as restaurant_business_hours
        from restaurant_order od
                 join restaurants restaurant on restaurant.id = od.restaurant_id
        where od.id = #{id}
    </select>

    <select id="queryOrdersByRestaurantIdAndStatus" resultMap="RestaurantOrderMapper">
        select od.id,
               od.date,
               od.total_price,
               od.type,
               od.completed,
               restaurant.id              as restaurant_id,
               restaurant.username        as restaurant_username,
               restaurant.password        as restaurant_password,
               restaurant.restaurant_name as restaurant_restaurant_name,
               restaurant.phone_number    as restaurant_phone_number,
               restaurant.email           as restaurant_email,
               restaurant.address         as restaurant_address,
               restaurant.description     as restaurant_description,
               restaurant.business_hours  as restaurant_business_hours
        from restaurant_order od
                 join restaurants restaurant on restaurant.id = od.restaurant_id
        where od.id = #{id}
          and od.completed = #{status}
    </select>

    <insert id="saveOrderProduct">
        insert into restaurant_order_product (order_id, product_id, amount)
        values (#{orderId}, #{productId}, #{amount})
    </insert>
</mapper>